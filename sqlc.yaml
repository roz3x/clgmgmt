version: "1"
packages:
  - name: "db"
    path: "db"
    schema: "./sql/schema/"
    queries:  "sql/query/"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: true
    emit_interface: false
    emit_exact_table_names: false
    emit_empty_slices: false